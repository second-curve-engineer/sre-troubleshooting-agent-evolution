# V2：基于框架的 Agent Workbench

这里先保留第二版的占位目录。

V2 的目标是从 V1 的硬编码 pipeline 继续往前走，尝试使用 Agent 框架搭建更完整的故障排查工作台，包括：

- 更丰富的工具集。
- 会话能力。
- 多工具协作。
- 前端 workbench。
- 更接近 Agent 应用的交互方式。

V2 带来的关键认知是：

> 框架可以帮助快速搭建 tool-using assistant，但生产故障排查场景仍然需要更强的 workflow 控制、上下文边界、trace 和 eval。

