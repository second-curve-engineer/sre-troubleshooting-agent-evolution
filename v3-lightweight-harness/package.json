{
  "name": "v3-lightweight-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "diagnose": "npm run build --silent && node --env-file=.env dist/cli.js diagnose",
    "dev": "npm run diagnose --",
    "eval": "npm run build --silent && node --env-file=.env dist/cli.js eval",
    "server": "npm run build --silent && node --env-file=.env dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "hitl-demo": "npm run build --silent && node --env-file=.env dist/cli.js hitl-demo"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
